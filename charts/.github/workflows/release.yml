name: release

# Publish charts to GHCR OCI (grouped under iterabase-charts/) and create a
# GitHub Release per chart tag with the packaged .tgz attached.
# Triggered by per-chart tags: <chart-name>-<semver>, e.g.
#   iterabase-platform-0.1.0, inference-gateway-0.1.0, control-plane-0.1.0
# The platform tag also publishes its same-version cert-manager-substrate companion.
# Charts are packaged with dependencies baked in (helm dependency build first).
# Data-dep subcharts (postgresql/redis/minio) are bundled only — not released.

on:
  push:
    tags:
      - "iterabase-platform-*"
      - "inference-gateway-*"
      - "control-plane-*"

permissions:
  contents: write # create GitHub Releases
  packages: write # push to GHCR

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: azure/setup-helm@v4
      - name: helm repo add external chart repositories
        run: |
          helm repo add ingress-nginx https://kubernetes.github.io/ingress-nginx
          helm repo add metallb https://metallb.github.io/metallb
          helm repo add jetstack https://charts.jetstack.io
          helm repo add external-dns https://kubernetes-sigs.github.io/external-dns/
          helm repo add stakater https://stakater.github.io/stakater-charts
          helm repo add prometheus-community https://prometheus-community.github.io/helm-charts
          helm repo add grafana https://grafana.github.io/helm-charts
      - name: resolve chart + version from tag
        id: tag
        run: |
          TAG="${GITHUB_REF##*/}"
          CHART="${TAG%-*}"
          VERSION="${TAG##*-}"
          echo "chart=$CHART" >> "$GITHUB_OUTPUT"
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
      - name: build dependencies
        env:
          CHART: ${{ steps.tag.outputs.chart }}
        run: |
          # control-plane and observability each carry their own nested deps
          # (postgresql; kube-prometheus-stack + loki). Build them first so the
          # umbrella vendors each subchart with nested deps baked in (matches
          # `make build-deps`); otherwise an observability-enabled umbrella
          # packages without the upstream archives.
          helm dependency build charts/control-plane
          helm dependency build charts/observability
          helm dependency build charts/cert-manager-substrate
          helm dependency build "charts/$CHART"
      - name: package + push
        env:
          CHART: ${{ steps.tag.outputs.chart }}
          VERSION: ${{ steps.tag.outputs.version }}
        run: |
          helm package "charts/$CHART" --version "$VERSION" --destination .cr-release
          if [ "$CHART" = "iterabase-platform" ]; then
            helm package charts/cert-manager-substrate --version "$VERSION" --destination .cr-release
          fi
          echo "${{ secrets.GITHUB_TOKEN }}" | helm registry login ghcr.io -u ${{ github.actor }} --password-stdin
          for pkg in .cr-release/*.tgz; do
            helm push "$pkg" "oci://ghcr.io/${{ github.repository_owner }}/iterabase-charts"
          done
      - name: github release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          files: .cr-release/*.tgz
          generate_release_notes: true
