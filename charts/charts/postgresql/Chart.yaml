apiVersion: v2
name: postgresql
description: Self-contained PostgreSQL for the iterabase platform (official image; optional internal-TLS via cert-manager).
type: application
version: 0.2.3
appVersion: "16"
