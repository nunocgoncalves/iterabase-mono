{{- if .Values.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "postgresql.secretName" . }}
  labels:
    {{- include "postgresql.labels" . | nindent 4 }}
type: Opaque
{{- $existing := lookup "v1" "Secret" .Release.Namespace (include "postgresql.secretName" .) }}
{{- if $existing }}
data:
{{- range $k, $v := $existing.data }}
  {{ $k }}: {{ $v }}
{{- end }}
{{- if and .Values.auth.gatewayUser.enabled (not (hasKey $existing.data "gateway-password")) }}
  gateway-password: {{ randAlphaNum 32 | b64enc | quote }}
{{- end }}
{{- else }}
stringData:
  password: {{ randAlphaNum 32 | quote }}
{{- if .Values.auth.gatewayUser.enabled }}
  gateway-password: {{ randAlphaNum 32 | quote }}
{{- end }}
{{- end }}
{{- end }}
