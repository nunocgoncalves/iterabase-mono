apiVersion: v2
name: metallb-config
description: MetalLB IPAddressPool + L2Advertisement config for Iterabase public and private ingress planes (L2 mode). The MetalLB operator itself is provided by the umbrella's community `metallb` dependency; this subchart only renders the pool CRs the operator reconciles.
type: application
version: 0.2.0
appVersion: "0.2.0"
