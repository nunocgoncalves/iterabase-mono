{{- include "rwx-storage-substrate.validate" . -}}
{{- if .Values.validation.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "rwx-storage-substrate.fullname" . }}-validation
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "rwx-storage-substrate.labels" . | nindent 4 }}
data:
  hor-424-rwx-conformance.sh: |-
    {{- .Files.Get "files/rwx-conformance.sh" | nindent 4 }}
  hor-424-rwx-conformance.yaml: |-
    {{- .Files.Get "files/rwx-conformance.yaml" | nindent 4 }}
  managed-profile-gate.sh: |-
    {{- .Files.Get "files/managed-profile-gate.sh" | nindent 4 }}
  uninstall-guard.sh: |-
    {{- .Files.Get "files/uninstall-guard.sh" | nindent 4 }}
{{- end }}
